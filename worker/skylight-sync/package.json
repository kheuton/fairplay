{
  "name": "skylight-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: FairPlay (Todoist) ⇄ Skylight kitchen calendar two-way sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240815.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.102.0"
  }
}
