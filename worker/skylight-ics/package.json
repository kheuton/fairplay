{
  "name": "skylight-ics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: FairPlay/Todoist chores → iCalendar (.ics) feed for Skylight",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240815.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0",
    "wrangler": "^3.0.0"
  }
}
